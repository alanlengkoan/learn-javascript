let character = 'Alan';
let age = 30;
let job = 'Web Developer';
let isBlackBelt = false;

const circ = (diameter) => {
    return diameter * Math.PI;
}

const whatType = (diameter) => {
    return typeof diameter;
}

console.log(circ(7.5));

console.log(whatType(isBlackBelt));
