const character = 'Alan';
const age = 30;
const job = 'Web Developer';

console.log(character, age, job);

const input = document.querySelectorAll('input');

input.forEach(element => {
    console.log(element);
    
});